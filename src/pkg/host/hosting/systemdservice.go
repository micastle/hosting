// systemd service works for linux platform only

// +build linux

package hosting

import (
	"goms.io/azureml/mir/mir-vmagent/pkg/host/dep"
)

func registerPlatformComponents(components dep.ComponentCollection) {
}
